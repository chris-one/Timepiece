// import the typedef from the typedef.js file//
const typeDefs = require('./typeDefs');

// import the resolver from the resolver.js file//
const resolvers = require('./resolvers');

// export the typeDefs and resolver from the //
module.exports = { typeDefs, resolvers };
